import "mocha";
import { expect } from "chai";
import { MockingDemo } from "../src/index";

describe("Mocking Demo:", () => {
	describe("When constructing", () => {

		beforeEach(() => {
			const demo = new MockingDemo();
		});

		it("the second heading is animated", () => {
		});
	});
});